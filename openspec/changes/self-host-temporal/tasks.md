# Tasks: Self-Hosted Temporal Server

- [ ] Add Temporal server to `docker-compose.dev.yml`
- [ ] Add Temporal server to `docker-compose.prod.yml`
- [ ] Update `backend` environment variables in `docker-compose.dev.yml`
- [ ] Update `temporal-worker` environment variables in `docker-compose.dev.yml`
- [ ] Update `backend` environment variables in `docker-compose.prod.yml`
- [ ] Update `temporal-worker` environment variables in `docker-compose.prod.yml`
- [ ] Verify connectivity between services and the new Temporal server
- [ ] Verify Temporal UI is accessible at `http://localhost:8080`
